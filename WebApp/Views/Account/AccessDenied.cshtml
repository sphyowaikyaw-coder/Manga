@{
    Layout = "MainLayout";
    ViewData["Title"] = "Access Denied";
}

<section class="auth-band">
    <div class="auth-card">
        <span class="eyebrow">Protected area</span>
        <h1>Access denied</h1>
        <p>Your account does not have permission to open that page.</p>
        <a class="pill-button" asp-controller="Manga" asp-action="Index">Back to Library</a>
    </div>
</section>
