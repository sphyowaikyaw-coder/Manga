@model WebApp.View_Model.MangaItem
@{
    Layout = "MainLayout";
    ViewData["Title"] = "Add Manga";
}

<section class="content-band dashboard-band">
    <div class="container">
        <div class="section-heading">
            <h2>Add Manga</h2>
            <a class="ghost-button" asp-controller="Admin" asp-action="Manga">Back to Manga</a>
        </div>
        <partial name="_MangaForm" model="Model" />
    </div>
</section>
